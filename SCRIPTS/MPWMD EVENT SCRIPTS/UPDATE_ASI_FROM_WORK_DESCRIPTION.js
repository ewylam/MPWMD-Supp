// Begin scipt to update work descrition
var vDescription = workDescGet(capId);
if (vDescription != null && vDescription != "") {
	editAppSpecific("Description of the issue", vDescription);
}
// End scipt to update work descrition
